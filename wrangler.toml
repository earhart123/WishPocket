name = "wishpocket"
compatibility_date = "2024-01-01"

# KV Namespace binding - This is required for data storage
# Run `npx wrangler kv:namespace create WISH_KV` to get the ID
[[kv_namespaces]]
binding = "WISH_KV"
id = "YOUR_KV_NAMESPACE_ID"

[site]
bucket = "./build"
